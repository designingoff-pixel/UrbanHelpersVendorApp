// Backend URL — not used anymore. All data flows through Firestore directly.
// Kept here to avoid import errors in any remaining references.
export const BACKEND_URL = "";
export const SOCKET_URL  = "";
